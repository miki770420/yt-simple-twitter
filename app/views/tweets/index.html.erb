tweet front gate

<div class="container">
  <div class="row">
    <%= @tweets.each do |tweet| %>
      <div class="col-md-8">
        <div class="col-md-2">
          <%= image_tag "http://lorempixel.com/400/200/human", class: "img-responsive img-circle" %>
        </div>
        <div class="col-md-6">
          <%= link_to tweet.user.name,  %>, <%= tweet.created_at %>

        
      